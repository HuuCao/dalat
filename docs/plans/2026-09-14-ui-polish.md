# UI Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Compact slot cards, a clearer "now" panel with a floating expense button and dated tabs, and a fund tab that says who gets refunded or tops up.

**Architecture:** Pure logic changes first (money format, time helpers, trip/status/fund models) with unit tests, then view + CSS changes task by task, each checked in headless Chrome. Form/Sheet labels (`Day N · …`) never change; display names come from new `day.title`.

**Tech Stack:** Vanilla ES modules, CSS, `node --test`, no build step, no dependencies. Headless Chrome via CDP for UI checks.

**Spec:** `docs/specs/2026-09-14-ui-polish-design.md`

## Global Constraints

- No build step, no dependency. Tests: `npm test` (Node ≥ 18).
- Mobile-first CSS; desktop overrides only inside `@media (min-width: 761px)`.
- Data text goes into the DOM only through `h()` / `textContent`, never HTML.
- `Element.replaceChildren()` never receives `null` — filter first.
- Form labels stay `Day N · <title>` / `Chung · <title>` / `… · Phát sinh`.
- Animation DOM contract stays: `.day-head` directly before `.timeline`; every `.item` a direct child of `.timeline`.
- Text kept as is: hero (`✦ Travel plan · Đà Lạt`, `— Let's go`, chips), footer, tags, `📍 Maps`.
- Tap targets ≥ 44px. Smallest text: countdown unit 10px; labels 11–12px; reading text 13px.
- Money: `< 1.000.000` → `640k`, `33,3k`; `≥ 1.000.000` → `11tr760`, `3tr065`, `12tr`.
- Commits: Conventional Commits, English, ending with `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

## Verification harness (used by Tasks 6–10)

Scratch script, not committed. `$SCRATCH` = `/private/tmp/claude-501/-Users-huucao-Projects-huucao/cd53a833-d7ef-4008-94ee-bb632c6d3de5/scratchpad`.

Start server once (background): `python3 -m http.server 8765 --bind 127.0.0.1` from repo root.

`$SCRATCH/shot.mjs` — `node shot.mjs <out-prefix> <url> <width> <mobile 0|1> [js-before-shot]`; env `PROBE=<js expression>` prints its value; env `SLICES=n` caps screenshots; env `MOTION=1` leaves animations on.

```js
import { spawn } from 'node:child_process';
import { writeFileSync, mkdtempSync } from 'node:fs';
import { join } from 'node:path';

const [out, url, width = '390', mobile = '1', pre = ''] = process.argv.slice(2);
const W = Number(width);
const H = mobile === '1' ? 844 : 900;
const port = 9300 + Math.floor(Math.random() * 500);
const dir = mkdtempSync(join(process.cwd(), 'prof-'));
const chrome = spawn('/Applications/Google Chrome.app/Contents/MacOS/Google Chrome', [
  '--headless=new', '--disable-gpu', '--hide-scrollbars', `--remote-debugging-port=${port}`,
  `--user-data-dir=${dir}`, 'about:blank',
], { stdio: 'ignore' });

const sleep = (ms) => new Promise((r) => setTimeout(r, ms));
let targets;
for (let i = 0; i < 50; i++) {
  try { targets = await (await fetch(`http://127.0.0.1:${port}/json`)).json(); break; } catch { await sleep(200); }
}
const page = targets.find((t) => t.type === 'page');
const ws = new WebSocket(page.webSocketDebuggerUrl);
await new Promise((r) => ws.addEventListener('open', r));
let id = 0;
const pending = new Map();
ws.addEventListener('message', (e) => {
  const msg = JSON.parse(e.data);
  if (msg.id && pending.has(msg.id)) { pending.get(msg.id)(msg); pending.delete(msg.id); }
});
const send = (method, params = {}) => new Promise((r) => { const i = ++id; pending.set(i, r); ws.send(JSON.stringify({ id: i, method, params })); });
const evalJs = async (expression) => (await send('Runtime.evaluate', { expression, awaitPromise: true, returnByValue: true })).result?.result?.value;

await send('Page.enable');
await send('Emulation.setDeviceMetricsOverride', { width: W, height: H, deviceScaleFactor: 1, mobile: mobile === '1' });
if (process.env.MOTION !== '1') {
  await send('Emulation.setEmulatedMedia', { features: [{ name: 'prefers-reduced-motion', value: 'reduce' }] });
}
await send('Page.navigate', { url });
await sleep(6000);
if (pre) { await evalJs(pre); await sleep(1500); }
console.log(`scrollHeight=${await evalJs('document.documentElement.scrollHeight')} scrollWidth=${await evalJs('document.documentElement.scrollWidth')} viewport=${W}`);
if (process.env.PROBE) console.log('probe:', JSON.stringify(await evalJs(process.env.PROBE)));
const total = await evalJs('document.documentElement.scrollHeight');
const cap = Number(process.env.SLICES ?? 99);
let n = 0;
for (let y = 0; y < total && n < cap; y += H) {
  await evalJs(`window.scrollTo(0, ${y}); new Promise(r => setTimeout(r, 400))`);
  const shot = await send('Page.captureScreenshot', { format: 'png' });
  writeFileSync(`${out}-${n++}.png`, Buffer.from(shot.result.data, 'base64'));
}
console.log(`slices=${n}`);
ws.close();
chrome.kill();
process.exit(0);
```

URLs: `B=http://127.0.0.1:8765/`; moments: `?now=2026-10-10T09:00:00%2B07:00` (soon), `?now=2026-10-16T12:00:00%2B07:00` (in a slot), `?now=2026-10-16T12:30:00%2B07:00` (between), `?now=2026-10-16T21:50:00%2B07:00` (next is tomorrow), `?now=2026-10-19T00:00:00%2B07:00` (done).

---

### Task 1: Money in millions

**Files:**
- Modify: `js/lib/money.js`
- Test: `tests/money.test.js`, `tests/fund.test.js`

**Interfaces:**
- Produces: `formatShort(amount: number): string` — new `tr` form ≥ 1.000.000. `formatDiff`, `formatBalance`, `formatFull` signatures unchanged.

- [ ] **Step 1: Write the failing tests** — in `tests/money.test.js` replace the first test with:

```js
test('formatShort writes thousands under a million', () => {
  assert.equal(formatShort(0), '0');
  assert.equal(formatShort(260_000), '260k');
  assert.equal(formatShort(33_333), '33,3k');
  assert.equal(formatShort(300), '0,3k');
  assert.equal(formatShort(999_949), '999,9k');
  assert.equal(formatShort(-200_000), '−200k');
});

test('formatShort writes millions and whole thousands from a million up', () => {
  assert.equal(formatShort(12_000_000), '12tr');
  assert.equal(formatShort(11_760_000), '11tr760');
  assert.equal(formatShort(3_065_000), '3tr065');
  assert.equal(formatShort(2_800_000), '2tr800');
  assert.equal(formatShort(1_660_500), '1tr661');
  assert.equal(formatShort(999_960), '1tr');
  assert.equal(formatShort(1_999_600), '2tr');
  assert.equal(formatShort(1_200_000_000), '1.200tr');
  assert.equal(formatShort(-1_200_000), '−1tr200');
});
```

In the last test of the same file add `assert.deepEqual(formatDiff(3_000_000, 1_800_000), { text: '▲ 1tr200', tone: 'over' });` and change `assert.equal(formatBalance(2_695_000), '+2.695k');` to `'+2tr695'`.

In `tests/fund.test.js`:
- `describeDay` test first line: `'💰 1.560k / 3.120k · phát sinh 120k'` → `'💰 1tr560 / 3tr120 · phát sinh 120k'`.
- settlement test: `2.695k` → `2tr695`, `2.155k` → `2tr155`, `1.615k` → `1tr615` (both), `1.385k` → `1tr385` (both); `845k`, `695k` unchanged (both occurrences of the Hữu text, including `settlement[0]`).

- [ ] **Step 2: Run to verify failure**

Run: `npm test`
Expected: FAIL in `money.test.js` (`'12.000k' !== '12tr'`) and `fund.test.js`.

- [ ] **Step 3: Implement** — replace `formatShort` in `js/lib/money.js`:

```js
const MINUS = '−';
const MILLION = 1_000_000;

const groupThousands = (value) => String(value).replace(/\B(?=(\d{3})+(?!\d))/g, '.');

// Totals and chips. Under a million: thousands of dong, one decimal at most
// ("33,3k"). From a million: millions and whole thousands ("11tr760", "12tr").
export function formatShort(amount) {
  if (amount === 0) return '0';
  const sign = amount < 0 ? MINUS : '';
  const hundreds = Math.round(Math.abs(amount) / 100);
  if (hundreds * 100 < MILLION) {
    const whole = Math.floor(hundreds / 10);
    const fraction = hundreds % 10;
    return `${sign}${groupThousands(whole)}${fraction ? `,${fraction}` : ''}k`;
  }
  const thousands = Math.round(Math.abs(amount) / 1000);
  const rest = thousands % 1000;
  return `${sign}${groupThousands(Math.floor(thousands / 1000))}tr${rest ? String(rest).padStart(3, '0') : ''}`;
}
```

- [ ] **Step 4: Run to verify pass**

Run: `npm test`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add js/lib/money.js tests/money.test.js tests/fund.test.js
git commit -m "feat: write amounts from a million as 11tr760"
```

---

### Task 2: Time helpers

**Files:**
- Modify: `js/lib/time.js`
- Test: `tests/time.test.js`, `tests/trip.test.js:53`

**Interfaces:**
- Produces:
  - `derivePeriod(start, end): string` → `'Sáng'`, `'Trưa → chiều'` (no `Buổi`).
  - `shortWeekdayText(date: 'YYYY-MM-DD'): string` → `'T6'`, `'CN'`.
  - `shortDateText(date): string` → `'T6 16/10'`.
  - `durationText(ms: number): string` → `'< 1 phút'`, `'15 phút'`, `'2 giờ'`, `'1 giờ 20 phút'` (minutes rounded up).
  - `localDateOf(ms: number, timezone: '+07:00'): string` → `'YYYY-MM-DD'` in that offset.

- [ ] **Step 1: Write the failing tests** — `tests/time.test.js`: import line becomes

```js
import {
  isTime, isDate, isTimezone, toMinutes, toDate,
  weekdayText, dateText, dateRangeText, derivePeriod, fillTemplate,
  shortWeekdayText, shortDateText, durationText, localDateOf,
} from '../js/lib/time.js';
```

In `derivePeriod` cases replace `'Buổi sáng'` → `'Sáng'`, `'Buổi trưa'` → `'Trưa'`, `'Buổi chiều'` → `'Chiều'`, `'Buổi tối'` → `'Tối'` (rename test to `'derivePeriod names the part of the day'`). Append:

```js
test('short weekday and date for tabs', () => {
  assert.equal(shortWeekdayText('2026-10-16'), 'T6');
  assert.equal(shortWeekdayText('2026-10-18'), 'CN');
  assert.equal(shortDateText('2026-10-16'), 'T6 16/10');
  assert.equal(shortDateText('2026-10-18'), 'CN 18/10');
  assert.equal(shortDateText('2026-03-05'), 'T5 05/03');
});

test('durationText rounds up to the minute', () => {
  assert.equal(durationText(30_000), '< 1 phút');
  assert.equal(durationText(60_000), '1 phút');
  assert.equal(durationText(61_000), '2 phút');
  assert.equal(durationText(59 * 60_000), '59 phút');
  assert.equal(durationText(59 * 60_000 + 1), '1 giờ');
  assert.equal(durationText(2 * 3_600_000), '2 giờ');
  assert.equal(durationText(80 * 60_000), '1 giờ 20 phút');
});

test('localDateOf reads the calendar date in the trip timezone', () => {
  assert.equal(localDateOf(Date.parse('2026-10-16T16:59:00Z'), '+07:00'), '2026-10-16');
  assert.equal(localDateOf(Date.parse('2026-10-16T17:00:00Z'), '+07:00'), '2026-10-17');
  assert.equal(localDateOf(Date.parse('2026-10-17T03:00:00Z'), '-03:30'), '2026-10-16');
});
```

In `tests/trip.test.js` change `assert.equal(first.period, 'Buổi sáng');` → `'Sáng'`.

- [ ] **Step 2: Run to verify failure**

Run: `npm test`
Expected: FAIL — `shortWeekdayText is not a function` / `'Buổi sáng' !== 'Sáng'`.

- [ ] **Step 3: Implement** in `js/lib/time.js`:

After `dateText` add:

```js
// "T6", "CN": weekday for tight spots such as the day tabs.
export function shortWeekdayText(date) {
  return weekdayText(date).replace('Th ', 'T');
}

export function shortDateText(date) {
  const [, month, day] = date.split('-');
  return `${shortWeekdayText(date)} ${day}/${month}`;
}
```

Replace `derivePeriod`:

```js
const capitalize = (text) => `${text[0].toUpperCase()}${text.slice(1)}`;

// The end minute is exclusive: 11:00–13:00 is still "Trưa".
export function derivePeriod(start, end) {
  const first = periodAt(toMinutes(start));
  const last = periodAt(toMinutes(end) - 1);
  return first === last ? capitalize(first) : `${capitalize(first)} → ${last}`;
}
```

Append:

```js
// Time left, rounded up so "còn 1 phút" never shows once the minute is gone.
export function durationText(ms) {
  if (ms < 60_000) return '< 1 phút';
  const minutes = Math.ceil(ms / 60_000);
  const hours = Math.floor(minutes / 60);
  const rest = minutes % 60;
  if (hours === 0) return `${rest} phút`;
  return rest === 0 ? `${hours} giờ` : `${hours} giờ ${rest} phút`;
}

function offsetMinutes(timezone) {
  const sign = timezone.startsWith('-') ? -1 : 1;
  const [hours, minutes] = timezone.slice(1).split(':').map(Number);
  return sign * (hours * 60 + minutes);
}

// The trip's calendar date at an instant, whatever the viewer's timezone.
export function localDateOf(ms, timezone) {
  return new Date(ms + offsetMinutes(timezone) * 60_000).toISOString().slice(0, 10);
}
```

- [ ] **Step 4: Run to verify pass**

Run: `npm test`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add js/lib/time.js tests/time.test.js tests/trip.test.js
git commit -m "feat: add short dates, durations and trip-local dates"
```

---

### Task 3: Display titles for days

**Files:**
- Modify: `js/model/trip.js:1-4,66-90`, `js/model/fund.js:172-183`
- Test: `tests/trip.test.js`, `tests/fund.test.js:80-84`

**Interfaces:**
- Consumes: `shortDateText` (Task 2).
- Produces: `day.title: 'Ngày N'`, `day.shortDate: 'T6 16/10'`; `ledger.days[].title`. `day.label` stays `'Day N'`.

- [ ] **Step 1: Write the failing tests** — append to `tests/trip.test.js`:

```js
test('days get a display title and short date; form labels keep "Day N"', () => {
  assert.deepEqual(trip.days.map(({ title, shortDate }) => ({ title, shortDate })), [
    { title: 'Ngày 1', shortDate: 'T6 16/10' },
    { title: 'Ngày 2', shortDate: 'T7 17/10' },
    { title: 'Ngày 3', shortDate: 'CN 18/10' },
  ]);
  assert.equal(trip.days[0].label, 'Day 1');
  assert.equal(trip.days[0].items[1].formLabel, 'Day 1 · Hidden Land');
  assert.equal(trip.days[0].extraLabel, 'Day 1 · Phát sinh');
});
```

In `tests/fund.test.js` replace the `ledger.days` assertion with:

```js
  assert.deepEqual(ledger.days.map(({ id, label, title, budget, actual, extra }) => ({ id, label, title, budget, actual, extra })), [
    { id: 'day-1', label: 'Day 1', title: 'Ngày 1', budget: 3_120_000, actual: 1_660_000, extra: 120_000 },
    { id: 'day-2', label: 'Day 2', title: 'Ngày 2', budget: 3_040_000, actual: 0, extra: 0 },
    { id: 'day-3', label: 'Day 3', title: 'Ngày 3', budget: 920_000, actual: 0, extra: 0 },
  ]);
```

- [ ] **Step 2: Run to verify failure**

Run: `npm test`
Expected: FAIL — `title: undefined`.

- [ ] **Step 3: Implement**

`js/model/trip.js` import: add `shortDateText` to the list from `'../lib/time.js'`. In `buildDay` return object, after `label,` add:

```js
    // Shown on the page. `label` stays "Day N": expense form answers use it.
    title: `Ngày ${index}`,
    shortDate: shortDateText(day.date),
```

`js/model/fund.js` in `buildLedger`, `trip.days.map` return object: after `label: day.label,` add `title: day.title,`.

- [ ] **Step 4: Run to verify pass**

Run: `npm test`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add js/model/trip.js js/model/fund.js tests/trip.test.js tests/fund.test.js
git commit -m "feat: give days a Vietnamese display title and short date"
```

---

### Task 4: Status says how long and what is next

**Files:**
- Modify: `js/model/status.js`
- Test: `tests/status.test.js`

**Interfaces:**
- Consumes: `durationText`, `shortWeekdayText` (Task 2).
- Produces: `getStatus(trip, now)` also returns `now`. `describeStatus(trip, status)` returns `{ label, tiles, headline, note, progress: string|null, next: string|null, mapUrl: string|null }`.

- [ ] **Step 1: Write the failing tests** — replace `tests/status.test.js` tests 1–5 (keep `countdownTiles` test) with:

```js
const quiet = { progress: null, next: null, mapUrl: null };

test('before the trip counts down', () => {
  const status = statusAt('2026-10-16T06:00:00+07:00');
  assert.equal(status.phase, 'soon');
  assert.equal(status.remainingMs, 3_600_000);
  assert.equal(status.current, null);
  assert.equal(status.next.id, 'day-1-item-1');
  assert.deepEqual(describeStatus(trip, status), {
    label: 'Đếm ngược khởi hành',
    tiles: [{ unit: 'giờ', value: '01' }, { unit: 'phút', value: '00' }, { unit: 'giây', value: '00' }],
    headline: null,
    note: 'Đà Lạt đang chờ · bắt đầu 07:00 · Th 6, 16/10',
    ...quiet,
  });
});

test('inside a slot shows that slot, time left and what comes next', () => {
  const status = statusAt('2026-10-16T08:00:00+07:00');
  assert.equal(status.phase, 'live');
  assert.equal(status.current.id, 'day-1-item-1');
  assert.equal(status.next.id, 'day-1-item-2');
  assert.deepEqual(describeStatus(trip, status), {
    label: 'Đang diễn ra',
    tiles: null,
    headline: '🍃 Đồi chè Cầu Đất — Săn mây, ăn sáng, cà phê',
    note: 'Còn 1 giờ · đến 09:00',
    progress: '0/16 điểm',
    next: 'Tiếp theo 09:15 · ☕ Hidden Land',
    mapUrl: trip.items[0].mapUrl,
  });
});

test('start is inclusive, end is exclusive; between slots shows the one ahead', () => {
  const atEnd = statusAt('2026-10-16T09:00:00+07:00');
  assert.equal(atEnd.current, null);
  assert.ok(atEnd.pastIds.has('day-1-item-1'));
  assert.deepEqual(describeStatus(trip, atEnd), {
    label: 'Đang di chuyển',
    tiles: null,
    headline: '☕ Hidden Land',
    note: 'Bắt đầu 09:15 · còn 15 phút',
    progress: '1/16 điểm',
    next: 'Tiếp theo 11:00 · 🍗 Gà nướng + Cơm lam',
    mapUrl: trip.items[1].mapUrl,
  });
  assert.equal(statusAt('2026-10-16T09:15:00+07:00').current.id, 'day-1-item-2');
});

test('empty slots are shown but not counted as places, and have no map', () => {
  assert.deepEqual(describeAt('2026-10-17T10:30:00+07:00'), {
    label: 'Đang diễn ra',
    tiles: null,
    headline: '☕ Cà phê — Tự do',
    note: 'Còn 45 phút · đến 11:15',
    progress: '11/16 điểm',
    next: 'Tiếp theo 11:30 · 🍚 Ăn trưa — Tự do',
    mapUrl: null,
  });
  const afterLunch = statusAt('2026-10-17T12:45:00+07:00');
  assert.equal(afterLunch.pastIds.size, 13);
  assert.equal(afterLunch.pastPlaces, 11);
  assert.equal(afterLunch.next.id, 'day-2-item-5');
});

test('what comes next names the weekday on another day, and is empty at the end', () => {
  const lateNight = describeAt('2026-10-16T21:50:00+07:00');
  assert.equal(lateNight.note, 'Còn 10 phút · đến 22:00');
  assert.equal(lateNight.next, 'Tiếp theo T7 07:00 · 🥞 Ăn sáng — Bánh căn');
  const goHome = describeAt('2026-10-18T12:30:00+07:00');
  assert.equal(goHome.note, 'Còn 30 phút · đến 13:00');
  assert.equal(goHome.next, null);
  assert.equal(goHome.mapUrl, null);
});

test('after the trip says goodbye', () => {
  const status = statusAt('2026-10-18T13:00:00+07:00');
  assert.equal(status.phase, 'done');
  assert.equal(status.pastIds.size, 21);
  assert.deepEqual(describeStatus(trip, status), {
    label: 'Hành trình đã khép lại', tiles: null, headline: 'Hẹn gặp lại Đà Lạt ✦', note: 'Đã đi qua 16 điểm trong 3 ngày', ...quiet,
  });
});
```

- [ ] **Step 2: Run to verify failure**

Run: `npm test`
Expected: FAIL in `status.test.js` (note text, missing keys).

- [ ] **Step 3: Implement** — `js/model/status.js`:

Top: `import { durationText, shortWeekdayText } from '../lib/time.js';`

In `getStatus` return: `return { phase, now, current, next, pastPlaces, pastIds, remainingMs: Math.max(trip.start.getTime() - now, 0) };`

Replace `describeStatus`:

```js
export function describeStatus(trip, status) {
  const place = trip.hero.title;
  const progress = `${status.pastPlaces}/${trip.placeCount} điểm`;
  const quiet = { progress: null, next: null, mapUrl: null };

  if (status.phase === 'soon') {
    const [first] = trip.items;
    const firstDay = trip.days.find((day) => day.id === first.dayId);
    return {
      label: 'Đếm ngược khởi hành',
      tiles: countdownTiles(status.remainingMs),
      headline: null,
      note: `${place} đang chờ · bắt đầu ${first.startText} · ${firstDay.dateText}`,
      ...quiet,
    };
  }

  if (status.phase === 'done') {
    return {
      label: 'Hành trình đã khép lại',
      tiles: null,
      headline: `Hẹn gặp lại ${place} ✦`,
      note: `Đã đi qua ${trip.placeCount} điểm trong ${trip.dayCount} ngày`,
      ...quiet,
    };
  }

  const { current, next, now } = status;
  if (current) {
    return {
      label: 'Đang diễn ra',
      tiles: null,
      headline: current.heading,
      note: `Còn ${durationText(current.end.getTime() - now)} · đến ${current.endText}`,
      progress,
      next: upNext(trip, current),
      mapUrl: current.mapUrl,
    };
  }

  if (next) {
    return {
      label: 'Đang di chuyển',
      tiles: null,
      headline: next.heading,
      note: `Bắt đầu ${next.startText} · còn ${durationText(next.start.getTime() - now)}`,
      progress,
      next: upNext(trip, next),
      mapUrl: next.mapUrl,
    };
  }

  return { label: 'Đang di chuyển', tiles: null, headline: 'Nghỉ giữa chặng', note: `Đã qua ${progress}`, ...quiet };
}

// The slot after `item`; on another day it names the weekday too.
function upNext(trip, item) {
  const after = trip.items[trip.items.indexOf(item) + 1];
  if (!after) return null;
  const day = trip.days.find((entry) => entry.id === after.dayId);
  const when = after.dayId === item.dayId ? after.startText : `${shortWeekdayText(day.date)} ${after.startText}`;
  return `Tiếp theo ${when} · ${after.heading}`;
}
```

- [ ] **Step 4: Run to verify pass**

Run: `npm test`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add js/model/status.js tests/status.test.js
git commit -m "feat: tell how long is left and what comes next in the trip status"
```

---

### Task 5: Fund wording that says who pays whom

**Files:**
- Modify: `js/model/fund.js` (`describeBucket`, `describeEntry`, settlement text, new `describeBalance`), `js/views/fund.js` (`fillMoney`)
- Test: `tests/fund.test.js`

**Interfaces:**
- Consumes: `formatShort` (Task 1).
- Produces:
  - `describeBucket(bucket, memberCount)` → adds `perPerson: string|null` (`'Mỗi người ~50k'` only when nothing spent and budget > 0); text `'Dự kiến 200k'`.
  - `describeEntry` → payer `'Khanh trả hộ'`; split `'chia đều 4 người'`.
  - `describeBalance(balance: number): { text: string, tone: 'under'|'over'|null }`.
  - `ledger.settlement[].text` uses `formatFull`.

- [ ] **Step 1: Write the failing tests** — `tests/fund.test.js`:

Import list add `describeBalance`.

Settlement test strings become (both occurrences of Hữu in the first block):

```js
    '→ Quỹ hoàn Hữu 2.695.000đ',
    '→ Quỹ hoàn Khanh 2.155.000đ',
    '→ Quỹ hoàn MiMi 1.615.000đ',
    '→ Quỹ hoàn Trâm 1.615.000đ',
```
`settlement[0]` → `text: '→ Quỹ hoàn Hữu 2.695.000đ'`; short block:
```js
    '→ MiMi nộp thêm vào quỹ 1.385.000đ',
    '→ Trâm nộp thêm vào quỹ 1.385.000đ',
    '→ Khanh nộp thêm vào quỹ 845.000đ',
    '→ Quỹ hoàn Hữu 695.000đ',
```

Replace `describeBucket` test:

```js
test('describeBucket covers every chip state', () => {
  const chip = (budget, actual) => describeBucket({ budget, actual }, 4);
  assert.deepEqual(chip(200000, 0), { icon: '💰', text: 'Dự kiến 200k', diff: null, tone: 'muted', perPerson: 'Mỗi người ~50k' });
  assert.deepEqual(chip(200000, 180000), { icon: '💰', text: '180k / 200k', diff: '▼ 20k', tone: 'under', perPerson: null });
  assert.deepEqual(chip(440000, 440000), { icon: '💰', text: '440k / 440k', diff: '✓', tone: 'under', perPerson: null });
  assert.deepEqual(chip(200000, 260000), { icon: '💰', text: '260k / 200k', diff: '▲ 60k', tone: 'over', perPerson: null });
  assert.deepEqual(chip(0, 0), { icon: '🆓', text: 'Miễn phí', diff: null, tone: 'muted', perPerson: null });
  assert.deepEqual(chip(0, 20000), { icon: '💰', text: '20k · ngoài dự kiến', diff: null, tone: 'over', perPerson: null });
  assert.deepEqual(chip(null, 0), { icon: '💰', text: 'Chưa chi', diff: null, tone: 'muted', perPerson: null });
  assert.deepEqual(chip(null, 540000), { icon: '💰', text: '540k', diff: null, tone: '', perPerson: null });
});
```

In `describeEntry` test: `detail: '4 nước + bánh · chia 4'` → `'4 nước + bánh · chia đều 4 người'`; `payer: 'Khanh ứng'` → `'Khanh trả hộ'`.

Append:

```js
test('describeBalance says refund, top up or even', () => {
  assert.deepEqual(describeBalance(3_065_000), { text: 'Hoàn 3tr065', tone: 'under' });
  assert.deepEqual(describeBalance(-845_000), { text: 'Nộp 845k', tone: 'over' });
  assert.deepEqual(describeBalance(0), { text: 'Đủ', tone: null });
});
```

- [ ] **Step 2: Run to verify failure**

Run: `npm test`
Expected: FAIL — `describeBalance is not a function`.

- [ ] **Step 3: Implement** — `js/model/fund.js`:

Settlement `text:`

```js
        text: balance > 0
          ? `→ Quỹ hoàn ${name} ${formatFull(balance)}`
          : `→ ${name} nộp thêm vào quỹ ${formatFull(-balance)}`,
```

Replace `describeBucket` and `describeEntry`:

```js
export function describeBucket({ budget, actual }, memberCount) {
  const view = (icon, text, diff, tone, perPerson = null) => ({ icon, text, diff, tone, perPerson });
  if (actual === 0) {
    if (budget === null) return view('💰', 'Chưa chi', null, 'muted');
    if (budget === 0) return view('🆓', 'Miễn phí', null, 'muted');
    return view('💰', `Dự kiến ${formatShort(budget)}`, null, 'muted', `Mỗi người ~${formatShort(Math.round(budget / memberCount))}`);
  }
  if (budget === null) return view('💰', formatShort(actual), null, '');
  if (budget === 0) return view('💰', `${formatShort(actual)} · ngoài dự kiến`, null, 'over');
  const diff = formatDiff(actual, budget);
  return view('💰', `${formatShort(actual)} / ${formatShort(budget)}`, diff.text, diff.tone);
}

export function describeEntry(entry, members) {
  let payer = `${entry.payer} trả ⚠️`;
  if (entry.payer === FUND_PAYER) payer = 'Quỹ trả';
  else if (members.includes(entry.payer)) payer = `${entry.payer} trả hộ`;

  const split = entry.splitFor.length === 0
    ? 'chia ?'
    : entry.splitFor.length === members.length
      ? `chia đều ${members.length} người`
      : `chia ${entry.splitFor.join(', ')}`;

  return {
    amount: formatFull(entry.amount),
    payer,
    detail: [entry.note, split].filter(Boolean).join(' · '),
    meta: [entry.enteredBy ? `${entry.enteredBy} nhập` : '', entry.time].filter(Boolean).join(' · '),
  };
}

// Settlement result: what the fund owes a member, or the member owes the fund.
export function describeBalance(balance) {
  if (balance > 0) return { text: `Hoàn ${formatShort(balance)}`, tone: 'under' };
  if (balance < 0) return { text: `Nộp ${formatShort(-balance)}`, tone: 'over' };
  return { text: 'Đủ', tone: null };
}
```

`js/views/fund.js` `fillMoney` body children become:

```js
  el.querySelector('.money-body').replaceChildren(...[
    note ? h('p', { class: 'money-note', text: note }) : null,
    view.perPerson ? h('p', { class: 'money-note', text: view.perPerson }) : null,
    renderEntries(bucket.entries, ctx),
    addLink(ctx.fund, bucket.label, '➕ Nhập chi ở đây'),
  ].filter(Boolean));
```

- [ ] **Step 4: Run to verify pass**

Run: `npm test`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add js/model/fund.js js/views/fund.js tests/fund.test.js
git commit -m "feat: word fund entries and settlement as refund or top up"
```

---

### Task 6: Compact slot cards

**Files:**
- Modify: `js/views/day.js`, `js/controllers/status.js`, `css/timeline.css`, `css/fund.css` (money line section), `data/trip.json:18`

**Interfaces:**
- Consumes: `day.title` (Task 3), `derivePeriod` output (Task 2), `perPerson` (Task 5).
- Produces: card DOM `.card-body > .card-title, .card-detail?, .card-meta?` where `.card-meta > .tag?, details.money?, a.map-btn?`; time-row tag `span.state-tag` (`.is-past` when past).

- [ ] **Step 1: Card DOM** — `js/views/day.js`:

Day title: `h('h2', { class: 'day-title', text: join(' ', day.icon, day.title) }),`

Replace `renderCard`:

```js
// Tag, money line and map link share one row that wraps when it runs out of
// room; past slots fold that row into the title line (timeline.css).
function renderCard(item, fund) {
  const meta = [
    !item.empty && item.tag ? h('span', { class: 'tag', dataset: { tag: item.tagKey }, text: item.tag }) : null,
    fund ? moneySlot(item.id) : null,
    !item.empty && item.mapUrl
      ? h('a', { class: 'map-btn', href: item.mapUrl, target: '_blank', rel: 'noopener noreferrer' }, '📍 Maps')
      : null,
  ].filter(Boolean);

  return h('div', { class: item.empty ? 'card empty' : 'card' },
    item.icon ? h('span', { class: 'card-icon', 'aria-hidden': 'true', text: item.icon }) : null,
    h('div', { class: 'card-body' },
      h('h3', { class: 'card-title', text: item.name }),
      item.detail ? h('p', { class: 'card-detail', text: item.detail }) : null,
      meta.length > 0 ? h('div', { class: 'card-meta' }, meta) : null));
}
```

- [ ] **Step 2: "Đã qua" tag** — `js/controllers/status.js`: in `tick` replace the loop body and `toggleNowTag`:

```js
    for (const [id, el] of elements) {
      const live = status.current?.id === id;
      const past = status.pastIds.has(id);
      el.classList.toggle('is-now', live);
      el.classList.toggle('is-past', past);
      setStateTag(el, live ? 'now' : past ? 'past' : null);
    }
```

```js
// "Đang diễn ra" / "Đã qua" beside the period, so state never rests on colour.
function setStateTag(el, state) {
  let tag = el.querySelector('.state-tag');
  if (!state) {
    tag?.remove();
    return;
  }
  if (!tag) {
    tag = h('span', { class: 'state-tag' });
    el.querySelector('.time').append(tag);
  }
  tag.textContent = state === 'now' ? 'Đang diễn ra' : 'Đã qua';
  tag.classList.toggle('is-past', state === 'past');
}
```

- [ ] **Step 3: Card CSS** — `css/timeline.css`:

1. `.day-date` font-size `12.5px` → `13px`.
2. `.time`: `margin-bottom: 8px` → `6px`; add `flex-wrap: wrap;`.
3. `.time .period`: `font-size: 12px; letter-spacing: .06em;` (rest unchanged).
4. `.card`: `gap: 10px; padding: 10px 12px;`.
5. `.card-icon`: `width: 34px; height: 34px; border-radius: 10px; font-size: 18px;`.
6. Replace the `.card-footer` rule with:

```css
.card-meta {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 2px 12px;
  margin-top: 6px;
}
```

7. `.tag`: `font-size: 12px; letter-spacing: .06em;`.
8. `.map-btn`: `margin: -12px 0 -12px auto; padding: 12px 0 12px 10px; font-size: 13px;`.
9. Replace `.now-tag` rule with:

```css
.state-tag {
  font-size: 12px;
  font-weight: 800;
  letter-spacing: .06em;
  text-transform: uppercase;
  color: var(--live-strong);
}

.state-tag.is-past { font-weight: 700; color: var(--text-sub); }
```

10. Replace the "Slots that are over" block (`.item.is-past …` 5 rules) with:

```css
/* Slots that are over fold to one line - name and money - on a neutral
   ground, so the eye lands on what is left. Opening the money line still
   shows the entries, on a row of their own. */
.item.is-past .card { align-items: center; padding-block: 8px; background: #f4f5f4; border-color: var(--line); }
.item.is-past .card-icon { width: 28px; height: 28px; border-radius: 8px; font-size: 15px; opacity: .6; }
.item.is-past .card-body { display: flex; flex-wrap: wrap; align-items: center; column-gap: 10px; }
.item.is-past .card-title { flex: 1 1 8rem; min-width: 0; font-size: 14px; color: var(--text-sub); }
.item.is-past .card-detail,
.item.is-past .tag,
.item.is-past .map-btn { display: none; }
.item.is-past .card-meta { display: contents; }
.item.is-past .clock { opacity: .55; }
.item.is-past .dot { background: var(--primary); box-shadow: 0 0 0 1px var(--primary); }
```

11. Desktop block: `.card-icon { width: 44px; height: 44px; border-radius: 11px; font-size: 23px; }` (keep other lines).

- [ ] **Step 4: Money line CSS** — `css/fund.css`, replace from `/* ---------- MONEY LINE` through `.money-note` with:

```css
/* ---------- MONEY LINE (cards and shared costs) ---------- */
/* In a card the line sits in .card-meta beside the tag; padding plus an equal
   negative margin keep a 44px tap target without a row of its own. */
.money { font-size: 13px; }

.money-sum {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 12px 0;
  margin: -12px 0;
  list-style: none;
  cursor: pointer;
  font-weight: 700;
  color: var(--text);
  font-variant-numeric: tabular-nums;
  -webkit-tap-highlight-color: transparent;
}

.money-sum::-webkit-details-marker { display: none; }

.money-sum::after {
  content: "▾";
  font-size: 12px;
  color: var(--link);
}

.money[open] > .money-sum::after { content: "▴"; }

/* Opened, the entries take a full row below the tag and map link. */
.money[open] { flex-basis: 100%; order: 3; }

/* Shared costs in the fund tab: one full-width row each. */
.fund-section > .money { border-top: 1px dashed var(--line); }
.fund-section > .money > .money-sum { min-height: 44px; padding: 0; margin: 0; }

.money-title { flex: 1 1 auto; min-width: 0; }
.money-title + .money-text { font-weight: 600; }
.money-diff { font-size: 12px; }

.money.is-muted .money-text { color: var(--money-muted); font-weight: 600; }
.money.is-over .money-text,
.money.is-over .money-diff { color: var(--money-over); }
.money.is-under .money-diff { color: var(--money-under); }

.money-body { padding: 6px 0 4px; }
.money-note { margin: 0 0 6px; font-size: 12px; color: var(--text-sub); }
```

Delete the line `.fund-section > .money:first-of-type { margin-top: 0; }`.

- [ ] **Step 5: Data** — `data/trip.json`: delete `"note": "Sáng → Trưa → Chiều → Tối",` from the first day.

- [ ] **Step 6: Unit tests still pass**

Run: `npm test`
Expected: all PASS.

- [ ] **Step 7: Browser check** (server running, see harness)

```bash
cd $SCRATCH && PROBE="[...document.querySelectorAll('#panel-day-1 .item')].map(i=>[i.dataset.id.slice(-6), i.classList.contains('is-past'), Math.round(i.querySelector('.card').getBoundingClientRect().height)])" \
  node shot.mjs t6 "http://127.0.0.1:8765/?now=2026-10-16T12:00:00%2B07:00" 390 1 "document.getElementById('tab-day-1').click()"
```

Expected: `scrollWidth=390`; past cards (`true`) ≤ 60px; other cards ≤ 110px (card only, time row excluded). Read `t6-0.png`…`t6-3.png`: one meta row per card, `ĐÃ QUA` / `ĐANG DIỄN RA` beside the period, grey past cards, chevron right after money text.

Then open a money line in a normal and a past card:

```bash
cd $SCRATCH && SLICES=2 node shot.mjs t6open "http://127.0.0.1:8765/?now=2026-10-16T12:00:00%2B07:00" 390 1 "document.getElementById('tab-day-1').click(); document.querySelectorAll('#panel-day-1 details.money').forEach((d,i)=>{ if(i<3) d.open=true }); window.scrollTo(0, document.querySelector('#panel-day-1 .timeline').getBoundingClientRect().top + scrollY - 80)"
```

Expected: opened entries sit on their own full-width row; tag and Maps stay on the row above; past card title row intact. Repeat first command at width 360 and 1440 (`0`): `scrollWidth` equals width.

- [ ] **Step 8: Commit**

```bash
git add js/views/day.js js/controllers/status.js css/timeline.css css/fund.css data/trip.json
git commit -m "feat: compact slot cards and fold past slots to one line"
```

---

### Task 7: Status panel, fund strip and floating button

**Files:**
- Modify: `js/views/countdown.js`, `css/countdown.css`, `js/views/fund.js` (status link, `fab`), `css/fund.css` (status row, `.fab`), `js/main.js`, `index.html:6`, `css/base.css` (`.wrap` safe area)

**Interfaces:**
- Consumes: `describeStatus` new fields (Task 4).
- Produces: `createFundView()` returns `{ panel, statusRow, fab: HTMLAnchorElement|null, update, fail, onRefresh }`.

- [ ] **Step 1: Panel DOM** — replace `createCountdown` in `js/views/countdown.js` (keep `HOURGLASS_SVG`, `renderHourglass`, `renderTile`):

```js
export function createCountdown() {
  const label = h('span', { class: 'cd-label-text' });
  const progress = h('span', { class: 'cd-progress' });
  const headline = h('div', { class: 'cd-headline' });
  const map = h('a', { class: 'cd-map', target: '_blank', rel: 'noopener noreferrer', hidden: true }, '📍 Maps');
  const headlineRow = h('div', { class: 'cd-headline-row' }, headline, map);
  const note = h('div', { class: 'cd-note' });
  const next = h('div', { class: 'cd-next', hidden: true });
  const clock = h('div', { class: 'cd-clock' });

  // Order of importance: what is happening, how long, what comes next.
  const element = h('section', { class: 'countdown', 'aria-label': 'Trạng thái chuyến đi' },
    h('div', { class: 'cd-glow', 'aria-hidden': 'true' }),
    renderHourglass(),
    h('div', { class: 'cd-body' },
      h('div', { class: 'cd-label' }, h('span', { class: 'pulse' }), label, progress),
      headlineRow,
      note),
    clock,
    next);

  function update(description, phase) {
    label.textContent = description.label;
    progress.textContent = description.progress ?? '';
    headline.textContent = description.headline ?? '';
    headlineRow.hidden = !description.headline;
    map.hidden = !description.mapUrl;
    if (description.mapUrl) map.href = description.mapUrl;
    note.textContent = description.note;
    next.textContent = description.next ?? '';
    next.hidden = !description.next;
    clock.hidden = !description.tiles;
    clock.replaceChildren(...(description.tiles ?? []).map(renderTile));
    element.classList.toggle('soon', phase === 'soon');
    element.classList.toggle('live', phase === 'live');
  }

  return { element, update };
}
```

- [ ] **Step 2: Panel CSS** — `css/countdown.css`:

1. `.cd-label`: `font-size: 11px;` (rest unchanged).
2. Replace `.cd-headline` rule with:

```css
.cd-progress {
  margin-left: auto;
  font-weight: 600;
  letter-spacing: .02em;
  text-transform: none;
  font-variant-numeric: tabular-nums;
}

.cd-headline-row {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-top: 4px;
}

.cd-headline {
  flex: 1 1 auto;
  min-width: 0;
  font-size: 16px;
  font-weight: 650;
  line-height: 1.3;
  letter-spacing: -.01em;
  color: #fff;
}

.cd-map {
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
  min-height: 44px;
  margin: -6px 0;
  padding: 0 12px;
  border-radius: 10px;
  background: rgba(255, 255, 255, .1);
  color: #fff;
  font-size: 13px;
  font-weight: 700;
  text-decoration: none;
}

.cd-map:focus-visible { outline: 2px solid var(--live); outline-offset: 2px; }

.cd-next {
  position: relative;
  flex: 1 0 100%;
  padding-top: 8px;
  border-top: 1px solid rgba(255, 255, 255, .1);
  font-size: 13px;
  color: #d7e3da;
}
```

3. `.cd-note`: `font-size: 13px; color: #b5c9bb;`.
4. `.cd-unit i`: `font-size: 10px;`; desktop `.cd-unit i { font-size: 10px; letter-spacing: .12em; }`.
5. Desktop block: delete `.cd-headline { font-size: 15px; }`.
6. Add inside `@media (hover: hover)` at end of file:

```css
@media (hover: hover) {
  .cd-map:hover { background: rgba(255, 255, 255, .18); }
}
```

- [ ] **Step 3: Fund view — `fab`** — `js/views/fund.js` in `createFundView` replace from `const statusLink` through `const statusRow` with:

```js
  const statusLink = addLink(fund, null, '➕ Nhập chi');
  // Phones get a floating copy in thumb reach (css/fund.css hides it on
  // desktop). Mounted outside <main>: an animated ancestor would break fixed.
  const fab = addLink(fund, null, '➕ Nhập chi');
  fab?.classList.replace('fund-btn', 'fab');
  // Worked out ahead of the tap, so the pre-filled place follows the clock.
  // Runs on pointerdown and focus too (not just click), so long-press "open
  // in new tab" and middle-click — which never fire a click event — still
  // get the current place.
  const updateLinkPlace = (event) => {
    event.currentTarget.href = formUrl(fund, placeLabelAt(trip, clock()));
  };
  for (const link of [statusLink, fab]) {
    for (const type of ['pointerdown', 'focus', 'click']) {
      link?.addEventListener(type, updateLinkPlace);
    }
  }
  const statusRow = h('div', { class: 'fund-status' }, statusText, statusLink);
```

Return object: add `fab,` after `statusRow,`.

- [ ] **Step 4: Mount** — `js/main.js` `mount()`:

```js
  const status = h('div', { class: fund ? 'status wrap has-fund' : 'status wrap' }, countdown.element, fund?.statusRow);
  const tabbar = renderTabs(trip.days, { fund: Boolean(fund) });
  const panels = [...trip.days.map((day) => renderDay(day, { fund: Boolean(fund) })), fund?.panel].filter(Boolean);
  const main = h('main', { class: fund?.fab ? 'wrap has-fab' : 'wrap' }, panels, renderFooter(trip.footer));

  app.replaceChildren(...[hero, status, tabbar, main, fund?.fab].filter(Boolean));
```

- [ ] **Step 5: Strip + FAB CSS** — `css/fund.css` replace the `/* ---------- STATUS ROW` section (comment, `.fund-status`, `.fund-status-text`) with:

```css
/* ---------- STATUS ROW ---------- */
/* Joined under the dark status panel as one block. It stays a separate
   element: the panel repaints every tick, the fund line does not. */
.status.has-fund .countdown { border-radius: 16px 16px 0 0; }

.fund-status {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  padding: 0 6px 0 14px;
  background: #16241d;
  border-top: 1px solid rgba(255, 255, 255, .1);
  border-radius: 0 0 16px 16px;
  box-shadow: 0 14px 30px rgba(16, 30, 25, .28);
}

.fund-status-text {
  min-height: 44px;
  display: flex;
  align-items: center;
  font-size: 13px;
  font-weight: 700;
  color: #e9f1eb;
  font-variant-numeric: tabular-nums;
}

/* Phones use the floating button instead. */
.fund-status .fund-btn { display: none; }

/* ---------- FLOATING BUTTON (phones) ---------- */
.fab {
  position: fixed;
  right: 16px;
  bottom: calc(16px + env(safe-area-inset-bottom));
  z-index: 25;
  display: inline-flex;
  align-items: center;
  min-height: 52px;
  padding: 0 20px;
  border-radius: 99px;
  background: var(--primary);
  color: #fff;
  font-size: 14px;
  font-weight: 700;
  text-decoration: none;
  box-shadow: 0 8px 24px rgba(16, 30, 25, .3);
  -webkit-tap-highlight-color: transparent;
}

.fab:focus-visible { outline: 2px solid var(--live); outline-offset: 3px; }

/* Room so the button never covers the last card or "Nhập phát sinh". */
main.has-fab { padding-bottom: 88px; }
```

Inside the existing `@media (hover: hover)` block add `.fund-status .fund-btn:hover { background: rgba(255, 255, 255, .18); }`.

Inside the existing `@media (min-width: 761px)` block add:

```css
  .fund-status .fund-btn {
    display: inline-flex;
    min-height: 36px;
    margin: 4px 0;
    background: rgba(255, 255, 255, .1);
    color: #fff;
  }
  .fab { display: none; }
  main.has-fab { padding-bottom: 48px; }
```

- [ ] **Step 6: Viewport + safe area** — `index.html` line 6: `content="width=device-width, initial-scale=1.0, viewport-fit=cover"`. `css/base.css`:

```css
.wrap { width: calc(100% - 24px - env(safe-area-inset-left) - env(safe-area-inset-right)); margin: 0 auto; }
```
and desktop: `.wrap { width: min(940px, calc(100% - 32px - env(safe-area-inset-left) - env(safe-area-inset-right))); }`.

- [ ] **Step 7: Unit tests**

Run: `npm test`
Expected: all PASS.

- [ ] **Step 8: Browser check** — for each moment (soon, in a slot, between, next is tomorrow, done):

```bash
cd $SCRATCH && SLICES=1 PROBE="({fab: document.querySelector('.fab')?.href, fabShown: !!document.querySelector('.fab')?.offsetParent, next: document.querySelector('.cd-next').hidden ? null : document.querySelector('.cd-next').textContent, map: document.querySelector('.cd-map').hidden ? null : 'shown'})" \
  node shot.mjs t7-live "http://127.0.0.1:8765/?now=2026-10-16T12:00:00%2B07:00" 390 1 "window.scrollTo(0,0)"
```

Expected per moment:
- soon: tiles, no headline row, `next: null`, `map: null`, unit labels readable.
- 12:00: `ĐANG DIỄN RA … 2/16 điểm`, headline `🍗 Gà nướng + Cơm lam`, `Còn 15 phút · đến 12:15`, `Tiếp theo 12:45 · 🐈 Trại Mèo Mướp`, Maps shown; strip `💰 Quỹ còn …` joined below with no light card; no `Nhập chi` in strip.
- 12:30: `ĐANG DI CHUYỂN`, `Bắt đầu 12:45 · còn 15 phút`.
- 21:50: `Tiếp theo T7 07:00 · 🥞 Ăn sáng — Bánh căn`.
- done: goodbye headline, no Maps, no next.
- `fabShown: true` at 390. The pre-filled place is set on `pointerdown` (a real click would open a tab), so check it by dispatching that event:

```bash
cd $SCRATCH && SLICES=0 PROBE="(document.querySelector('.fab').dispatchEvent(new PointerEvent('pointerdown')), decodeURIComponent(document.querySelector('.fab').href))" \
  node shot.mjs t7-fab "http://127.0.0.1:8765/?now=2026-10-16T12:00:00%2B07:00" 390 1
```
Expected: href ends with `Day 1 · Gà nướng + Cơm lam`.

Scroll to the bottom of Day 1 (`SLICES` default, tab Day 1): the floating button never covers `➕ Nhập phát sinh` in the last slice. At 1440: `fabShown: false`, strip shows `➕ Nhập chi` light-on-dark.

- [ ] **Step 9: Commit**

```bash
git add js/views/countdown.js css/countdown.css js/views/fund.js css/fund.css js/main.js index.html css/base.css
git commit -m "feat: reorder the status panel and add a floating expense button"
```

---

### Task 8: Dated tabs with a today marker

**Files:**
- Modify: `js/views/tabs.js`, `js/controllers/tabs.js`, `js/controllers/status.js`, `css/tabs.css`

**Interfaces:**
- Consumes: `day.title`, `day.shortDate`, `day.dateText` (Task 3); `localDateOf` (Task 2).
- Produces: `createTabs(...)` returns `{ select, markToday(dayId: string|null) }`.

- [ ] **Step 1: Tabs view** — replace `renderTabs` in `js/views/tabs.js`:

```js
export function renderTabs(days, { fund = false } = {}) {
  const panelId = (day) => `panel-${day.id}`;
  const tab = (id, controls, content, label) => h('button', {
    class: 'tab',
    type: 'button',
    role: 'tab',
    id: `tab-${id}`,
    'aria-controls': controls,
    'aria-label': label,
    dataset: { tab: id },
  }, content);

  return h('div', { class: 'tabbar' },
    h('div', { class: 'wrap' },
      h('div', { class: 'tabs', role: 'tablist', 'aria-label': 'Chọn ngày' },
        tab(ALL_TAB, days.map(panelId).join(' '), 'Tất cả'),
        days.map((day) => tab(day.id, panelId(day), [
          h('span', { class: 'tab-name', text: day.title }),
          h('span', { class: 'tab-date', text: day.shortDate }),
        ], `${day.title}, ${day.dateText}`)),
        fund ? tab(FUND_TAB, `panel-${FUND_TAB}`, '💰 Quỹ') : null)));
}
```

- [ ] **Step 2: `markToday`** — `js/controllers/tabs.js`, before `select(ALL_TAB);`:

```js
  const TODAY = ', hôm nay';

  // Lime dot on the day in progress; the clock calls this every tick, so the
  // dot moves on at midnight.
  function markToday(dayId) {
    for (const tab of tabs) {
      const today = tab.dataset.tab === dayId;
      if (today === tab.hasAttribute('data-today')) continue;
      tab.toggleAttribute('data-today', today);
      const label = tab.getAttribute('aria-label');
      if (label) tab.setAttribute('aria-label', today ? `${label}${TODAY}` : label.replace(TODAY, ''));
    }
  }
```

Return: `return { select, markToday };`

- [ ] **Step 3: Call it** — `js/controllers/status.js`: import `import { localDateOf } from '../lib/time.js';`. In `tick` replace `const status = getStatus(trip, clock());` with:

```js
    const now = clock();
    const status = getStatus(trip, now);
    const today = localDateOf(now, trip.timezone);
    tabs.markToday(trip.days.find((day) => day.date === today)?.id ?? null);
```

- [ ] **Step 4: Tabs CSS** — `css/tabs.css`: replace `.tab` rule with:

```css
.tab {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-width: 0;
  min-height: 52px;
  padding: 6px 4px;
  border: 0;
  border-radius: 10px;
  background: transparent;
  color: var(--text-sub);
  font: inherit;
  font-size: 12.5px;
  font-weight: 600;
  line-height: 1.25;
  text-align: center;
  cursor: pointer;
  transition: background .18s ease, color .18s ease;
  white-space: nowrap;
  -webkit-user-select: none;
  user-select: none;
  -webkit-tap-highlight-color: transparent;
}

.tab-name,
.tab-date {
  display: block;
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
}

.tab-date { font-size: 11px; font-weight: 500; opacity: .85; }

/* Today: a lime dot under the label. */
.tab[data-today]::after {
  content: "";
  position: absolute;
  bottom: 4px;
  left: 50%;
  width: 6px;
  height: 6px;
  margin-left: -3px;
  border-radius: 50%;
  background: var(--live);
}
```

Desktop block: `.tab { font-size: 13.5px; padding: 6px 8px; }`.

- [ ] **Step 5: Unit tests**

Run: `npm test`
Expected: all PASS.

- [ ] **Step 6: Browser check**

```bash
cd $SCRATCH && SLICES=1 PROBE="[...document.querySelectorAll('.tab')].map(t=>[t.textContent, t.getAttribute('aria-label'), t.hasAttribute('data-today'), Math.round(t.getBoundingClientRect().height)])" \
  node shot.mjs t8 "http://127.0.0.1:8765/?now=2026-10-17T12:00:00%2B07:00" 390 1 "window.scrollTo(0,0)"
```

Expected: day tabs `Ngày 1T6 16/10` etc., only `tab-day-2` has `data-today` and label `Ngày 2, Th 7, 17/10, hôm nay`; heights 52; `scrollWidth=390`. Repeat with `?now=2026-10-10T09:00:00%2B07:00` and `?now=2026-10-19T00:00:00%2B07:00`: no `data-today`. Repeat at width 360: date text not clipped (read screenshot).

- [ ] **Step 7: Commit**

```bash
git add js/views/tabs.js js/controllers/tabs.js js/controllers/status.js css/tabs.css
git commit -m "feat: show dates on day tabs and mark today"
```

---

### Task 9: Fund tab reorder, overview and settlement

**Files:**
- Modify: `js/views/fund.js` (`renderPanel`, `renderOverview`, `renderByDay`, `renderSettlement`, imports), `css/fund.css` (tiles, table), `js/lib/money.js` (drop `formatBalance`)
- Test: `tests/money.test.js`

**Interfaces:**
- Consumes: `describeBalance` (Task 5), `ledger.days[].title` (Task 3).

- [ ] **Step 1: Drop `formatBalance`** — `tests/money.test.js`: remove `formatBalance` from import and its three asserts (rename test `'formatDiff carries the direction'`). `js/lib/money.js`: delete `formatBalance`. `js/views/fund.js` imports:

```js
import { formatShort, formatFull } from '../lib/money.js';
import {
  describeBucket, describeEntry, describeDay, describeBalance, progressOf, formUrl, placeLabelAt,
} from '../model/fund.js';
```

- [ ] **Step 2: Reorder** — in `renderPanel` the article children become:

```js
    panel.replaceChildren(h('article', { class: 'fund' },
      renderHead(metaText(meta), meta.state !== 'unlinked', meta.error),
      // Things to fix first, then what people ask most: what is left, who
      // gets what back.
      ledger.warnings.length > 0 ? renderWarnings(ledger.warnings) : null,
      ledger.unmatched.entries.length > 0
        ? section('Không khớp địa điểm',
          details('unmatched', `⚠️ ${ledger.unmatched.entries.length} khoản · ${formatShort(ledger.unmatched.actual)}`,
            renderEntries(ledger.unmatched.entries, ctx, { withLabel: true })))
        : null,
      renderOverview(totals),
      renderSettlement(ledger),
      renderByDay(ledger),
      renderShared(ledger, ctx),
      section('Sổ sách',
        details('contributions', `Sổ góp quỹ · ${ledger.contributions.length} khoản · ${formatShort(totals.contributed)}`,
          renderContributions(ledger.contributions)),
        details('entries', `Sổ chi · ${ledger.entries.length} khoản · ${formatShort(totals.actual)}`,
          renderEntries(ledger.entries, ctx, { withLabel: true }))),
      h('div', { class: 'fund-actions' },
        addLink(fund, null, '➕ Nhập chi tiêu'),
        fund.sheet ? h('a', { class: 'fund-btn is-ghost', href: fund.sheet, ...LINK }, '📄 Mở Google Sheet') : null)));
```

- [ ] **Step 3: Overview** — replace `renderOverview`:

```js
function renderOverview(totals) {
  const tile = (label, value, className = 'fund-tile') => h('div', { class: className }, h('span', { text: label }), h('b', { text: value }));
  const short = totals.reserve < 0;
  return h('section', { class: 'fund-section' },
    tile('Quỹ còn', formatShort(totals.fundLeft), 'fund-tile is-main'),
    h('div', { class: 'fund-tiles' },
      tile('Đã góp', formatShort(totals.contributed)),
      tile('Đã chi', formatShort(totals.actual)),
      short
        ? tile('Thiếu so với dự kiến', formatShort(-totals.reserve), 'fund-tile is-over')
        : tile('Dư so với dự kiến', formatShort(totals.reserve))),
    h('div', { class: 'fund-progress' },
      h('span', { text: 'Thực chi / dự kiến' }),
      h('span', { text: `${formatShort(totals.actual)} / ${formatShort(totals.budget)}` })),
    progressBar(progressOf(totals.actual, totals.budget)));
}
```

- [ ] **Step 4: By day** — in `renderByDay`: `ledger.days.map((day) => row(day.title, [day.budget, day.actual, day.extra])),`

- [ ] **Step 5: Settlement** — replace `renderSettlement`:

```js
function renderSettlement(ledger) {
  const rows = ledger.people.map((person) => {
    const result = describeBalance(person.balance);
    return h('tr', {},
      h('th', { scope: 'row', text: person.name }),
      h('td', { text: formatShort(person.contributed) }),
      h('td', { text: formatShort(person.advanced) }),
      h('td', { text: formatShort(person.share) }),
      h('td', { class: result.tone ? `is-${result.tone}` : 'is-even', text: result.text }));
  });
  const foot = h('tr', {}, h('td', { colspan: '5', text: `Quỹ còn ${formatShort(ledger.totals.fundLeft)}` }));

  return section(ledger.done ? 'Quyết toán' : 'Quyết toán · tạm tính',
    ledger.excluded > 0
      ? h('p', { class: 'fund-warn-line', text: `⚠️ Có ${ledger.excluded} dòng cần sửa — số liệu chưa chốt` })
      : null,
    h('p', { class: 'fund-formula', text: 'Kết quả = Đã góp + Trả hộ − Phần chịu' }),
    table(['', 'Đã góp', 'Trả hộ', 'Phần chịu', 'Kết quả'], rows, foot, 'fund-table fund-settle'),
    ledger.settlement
      ? h('div', { class: 'fund-settlement' },
        h('h4', { text: 'Chốt quỹ' }),
        ledger.settlement.length > 0
          ? h('ul', {}, ledger.settlement.map((line) => h('li', { text: line.text })))
          : h('p', { text: 'Không ai cần chuyển thêm' }))
      : null);
}
```

- [ ] **Step 6: CSS** — `css/fund.css`:

Replace `.fund-tiles`, `.fund-tile`, `.fund-tile span`, `.fund-tile b` rules with:

```css
.fund-tiles {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 8px;
}

.fund-tile { padding: 10px 12px; border-radius: 12px; background: var(--accent-soft); }
.fund-tile span { display: block; font-size: 12px; font-weight: 600; line-height: 1.3; color: var(--text-sub); }
.fund-tile b { font-size: 16px; color: var(--primary); font-variant-numeric: tabular-nums; }
.fund-tile.is-over b { color: var(--money-over); }

/* What is left in the fund: the number people look for first. */
.fund-tile.is-main {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 8px;
  padding: 12px 14px;
}

.fund-tile.is-main span { font-size: 13px; font-weight: 700; color: var(--primary); }
.fund-tile.is-main b { font-size: 24px; }
```

After `.fund-table .is-under` add:

```css
.fund-table .is-even { color: var(--text-sub); font-weight: 700; }
.fund-formula { margin: 0 0 6px; font-size: 12px; color: var(--text-sub); }
```

Desktop block: replace `.fund-tiles { grid-template-columns: repeat(4, minmax(0, 1fr)); }` with `.fund-tile b { font-size: 18px; }`. (`.fund-tile.is-main b` is more specific, so the main tile stays 24px.)

- [ ] **Step 7: Unit tests**

Run: `npm test`
Expected: all PASS; `grep -rn formatBalance js tests` prints nothing.

- [ ] **Step 8: Browser check**

```bash
cd $SCRATCH && PROBE="[...document.querySelectorAll('#panel-fund .fund-h')].map(e=>e.textContent)" \
  node shot.mjs t9 "http://127.0.0.1:8765/?now=2026-10-16T12:00:00%2B07:00" 390 1 "document.getElementById('tab-fund').click(); window.scrollTo(0,0)"
```

Expected probe order: (`Không khớp địa điểm` if any) → `Quyết toán · tạm tính` → `Theo ngày` → `Chi phí chung` → `Sổ sách`. Screenshots: big `Quỹ còn 11tr…` tile, 3 small tiles, formula line, `Hoàn …` green / `Nộp …` orange / `Đủ` grey, table rows `Ngày 1…3`, no horizontal page scroll (`scrollWidth=390`; table may scroll inside its wrapper). Repeat with `?now=2026-10-19T00:00:00%2B07:00`: title `Quyết toán`, `Chốt quỹ` lines end with `đ`. Repeat at 360.

- [ ] **Step 9: Commit**

```bash
git add js/views/fund.js css/fund.css js/lib/money.js tests/money.test.js
git commit -m "feat: lead the fund tab with what is left and who gets what back"
```

---

### Task 10: README and full pass

**Files:**
- Modify: `README.md`

- [ ] **Step 1: README edits** (exact new wording):

- §2 bullet 1 → `- Tab \`Tất cả\`, \`Ngày 1…N\` và \`💰 Quỹ\` sinh tự động theo dữ liệu. Tab ngày ghi thêm thứ và ngày (\`T6 16/10\`); ngày đang diễn ra có chấm xanh dưới chữ. Thêm ngày thứ 4 thì tab thứ 4 tự xuất hiện.`
- §3 `**Tiêu đề:** icon + \`Day N\`.` → `icon + \`Ngày N\`.`; example `\`Th 6, 16/10 · 07:00 → 22:00 · Sáng → Trưa → Chiều → Tối\`` → `\`Th 7, 17/10 · 07:00 → 21:30 · Cà phê · Lẩu · Chợ Đà Lạt\``.
- §3 buổi: `\`Buổi sáng\`, \`Buổi trưa\`, \`Buổi chiều\`, \`Buổi tối\`, hoặc khung vắt qua hai buổi như \`Trưa → chiều\`` → `\`Sáng\`, \`Trưa\`, \`Chiều\`, \`Tối\`, hoặc khung vắt qua hai buổi như \`Trưa → chiều\``.
- §3 after the "Thẻ địa điểm" bullet add: `- **Một hàng thông tin:** nhãn phân loại, dòng 💰 và link \`📍 Maps\` nằm chung một hàng, hẹp thì tự xuống dòng.`
- §4 table rows:
  - `Đang ở một khung giờ` → `Tên điểm hiện tại + nút \`📍 Maps\`, ghi chú \`Còn 15 phút · đến 12:15\`, dòng \`Tiếp theo 12:45 · 🐈 Trại Mèo Mướp\`, tiến độ \`2/16 điểm\` ở góc phải, chấm xanh nhấp nháy`
  - `Giữa hai khung giờ` → `Tên điểm kế tiếp + nút \`📍 Maps\`, ghi chú \`Bắt đầu 09:15 · còn 15 phút\`, dòng Tiếp theo, tiến độ`
  - add bullets: `- Điểm tiếp theo ở ngày khác thì ghi thêm thứ: \`Tiếp theo T7 07:00 · 🥞 Ăn sáng — Bánh căn\`.` and `- Hàng \`💰 Quỹ còn\` dính liền dưới panel thành một khối.`
- §5 `**Khung đã qua** tự mờ đi: …` → `- **Khung đã qua** thu gọn: thẻ còn một hàng (tên + dòng 💰) trên nền xám, nhãn \`ĐÃ QUA\`, giờ mờ, chấm đổi màu đậm — vẫn bấm được để xem các khoản chi.`
- §7 `tab cao tối thiểu 44px` → `tab cao tối thiểu 52px`; add bullet `- **Nút nổi \`➕ Nhập chi\`** ở góc dưới phải trong tầm ngón cái, tránh thanh home của iPhone; chỉ hiện trên điện thoại.`
- §10:
  - `\`💰 Dự kiến 200k · 50k/người\` (chưa chi)` → `\`💰 Dự kiến 200k\` (chưa chi; mở ra thấy \`Mỗi người ~50k\`)`
  - `\`💰 1.660k / 3.120k · phát sinh 120k\`` → `\`💰 1tr660 / 3tr120 · phát sinh 120k\``
  - `**Dưới panel trạng thái:** \`💰 Quỹ còn 8.080k\` và nút \`➕ Nhập chi\`` → `**Dính liền panel trạng thái:** \`💰 Quỹ còn 8tr080\`; nút \`➕ Nhập chi\` trên máy tính, nút nổi trên điện thoại`
  - Tab Quỹ bullet → `- **Tab \`💰 Quỹ\`:** cảnh báo và khoản không khớp địa điểm (nếu có); quỹ còn (ô lớn) · đã góp · đã chi · dư/thiếu so với dự kiến; quyết toán; bảng theo ngày; chi phí chung; sổ góp quỹ và sổ chi; nút mở Form và Sheet. Tab \`Tất cả\` không gồm tab này.`
  - Quyết toán bullet → `- **Quyết toán:** \`Kết quả = Đã góp + Trả hộ − Phần chịu\`, ghi bằng chữ: \`Hoàn 2tr695\`, \`Nộp 845k\`, \`Đủ\`; tổng các kết quả luôn bằng số quỹ còn. Trong chuyến ghi \`tạm tính\`; khi chuyến đi kết thúc hiện \`Chốt quỹ\` ghi đủ từng đồng — \`→ Quỹ hoàn Hữu 2.695.000đ\`, \`→ Khanh nộp thêm vào quỹ 845.000đ\` — mỗi người chỉ một giao dịch với quỹ.`
  - add bullet `- **Cách viết tiền:** dưới 1 triệu \`640k\`, \`33,3k\`; từ 1 triệu \`11tr760\`, \`12tr\`; từng khoản chi và chốt quỹ ghi đủ đồng \`260.000đ\`.`
- Test table:
  - `time.test.js` → add `, thứ ngắn \`T6 16/10\`, thời lượng \`1 giờ 20 phút\`, ngày theo múi giờ chuyến đi`
  - `status.test.js` → add `, thời gian còn lại, điểm tiếp theo (cả sang ngày khác), link Maps`
  - `money.test.js` → `\`1tr660\`, \`12tr\`, \`33,3k\`, \`260.000đ\`, ▲ ▼ ✓`
  - `fund.test.js` → add `, chữ Hoàn / Nộp / Đủ`
- Tài liệu list add: `- [Thiết kế tinh gọn giao diện](docs/specs/2026-09-14-ui-polish-design.md) · [Kế hoạch](docs/plans/2026-09-14-ui-polish.md).`

- [ ] **Step 2: Full pass** — `npm test` all PASS. Then for widths 360, 390, 430 (mobile `1`) and 1440 (`0`), each moment URL, on tab `Tất cả`, run `SLICES=1 node shot.mjs full-<w>-<m> <url> <w> <m>` and check `scrollWidth` equals width. At 390 with `MOTION=1` switch tabs (`document.getElementById('tab-day-2').click()`) and confirm cards appear (no stuck `opacity: 0`: probe `[...document.querySelectorAll('#panel-day-2 .item')].every(i=>getComputedStyle(i).opacity==='1')` after the 1.5s wait — expected `true`). Tap targets:

```bash
cd $SCRATCH && SLICES=0 PROBE="[...document.querySelectorAll('.tab, .map-btn, .money-sum, .fab, .cd-map')].filter(e=>e.offsetParent).map(e=>[e.className, Math.round(e.getBoundingClientRect().height)]).filter(([,h])=>h<44)" \
  node shot.mjs tap "http://127.0.0.1:8765/?now=2026-10-16T12:00:00%2B07:00" 390 1 "document.getElementById('tab-day-1').click()"
```
Expected: `probe: []`.

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: describe the compact cards, status panel and fund tab"
```
